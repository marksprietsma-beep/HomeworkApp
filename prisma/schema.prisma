generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model LocalDatabaseCheck {
  id        Int      @id @default(autoincrement())
  label     String   @default("local database baseline")
  createdAt DateTime @default(now())
}
